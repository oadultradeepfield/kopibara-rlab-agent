# Kopibara Agent dashboard

Small read-only dashboard for the completed KuaiRand-Pure autonomous run.
The committed snapshot lives in `src/data.ts`; it is deliberately static so the
frontend has no backend or credentials to configure.

```bash
npm install
npm run dev
```

Run `npm run check` and `npm run build` before committing frontend changes.
